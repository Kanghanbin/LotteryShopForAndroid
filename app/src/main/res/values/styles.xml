<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/updatPwd_bg</item>
        <item name="colorPrimaryDark">@color/updatPwd_bg</item>
        <item name="colorAccent">@color/updatPwd_bg</item>
    </style>
    <style name="AppBaseTheme" parent="AppTheme"></style>

    <style name="CustomCheckBoxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style</item>
    </style>

    <style name="style_RadioButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:background">@null</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@drawable/selector_tab_text_color</item>
        <item name="android:textSize">12sp</item>
    </style>


    <style name="ActionSheetDialog" parent="android:Theme.Dialog">
        <item name="android:windowContentOverlay">@null</item>//设置窗体内容背景
        <item name="android:windowFrame">@null</item>//是否有边框
        <item name="android:windowBackground">@android:color/transparent</item>//窗体的背景
        <item name="android:windowNoTitle">true</item>//窗体是否有背景
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
